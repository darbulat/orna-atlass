# UI evals

The registered browser suite is `web/e2e`. It covers deterministic public, authentication, player
and failure journeys through the mock API. Real-stack codec/network and automated axe coverage
remain explicit limitations in `docs/CURRENT_STATE.md`.
