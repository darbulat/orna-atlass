# Performance evals

The registered PostGIS scale eval reuses `tests/integration/test_postgis_atlas.py`. Audio pipeline
measurements remain governed by `docs/PERFORMANCE_BASELINE.md`; they require explicit disposable
dependencies and are not part of the dependency-free fast tier.
